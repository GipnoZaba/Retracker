import React from "react";
import { observer } from "mobx-react-lite";

const ProjectFrom = () => {
  return (
    <div style={{left: "100px", height: "100px"}}>lllllllll</div>
  );
};

export default observer(ProjectFrom);
